function multiply(firstNumber, secondNumber) {
    let result = firstNumber + secondNumber;
    return result;
}

console.log( multiply(3, 5) );

let wynikSuma = multiply(10, 5);

console.log(wynikSuma);